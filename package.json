{
  "name": "CC2-WebUI",
  "version": "0.0.1",
  "private": false,
  "scripts": {
    "start": "bun index.ts",
    "build": "bun tsc --outDir dist",
    "setup": "bun install && cd src/Frontend && bun install && bun run build"
  },
  "dependencies": {
    "cluster": "^0.7.7",
    "sqlite3": "5.1.6",
    "multer": "2.2.0",
    "crypto": "^1.0.1",
    "mqtt": "^4.3.7"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "@types/node": "^20.4.2",
    "typescript": "^5.0.0",
    "dotenv": "^16.0.0"
  }
}