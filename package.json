{
  "name": "CC2-WebUI",
  "version": "0.0.2",
  "private": false,
  "scripts": {
    "start": "bun index.ts",
    "build": "bun tsc --outDir dist"
  },
  "dependencies": {
    "cluster": "^0.7.7"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "typescript": "^5.0.0",
    "dotenv": "^16.0.0"
  }
}