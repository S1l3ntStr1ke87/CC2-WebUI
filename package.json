{
  "name": "CC2-WebUI",
  "version": "0.0.1",
  "description": "A Simple LAN WebUI For the Centauri Carbon 2",
  "private": false,
  "scripts": {
    "start": "bun index.ts",
    "dev": "cd src/Frontend && bun dev",
    "vitebuild": "cd src/Frontend && bun run build",
    "preview": "cd src/Frontend && bun run preview",
    "setup": "bun install && cd src/Frontend && bun install && bun run build"
  },
  "dependencies": {
    "cluster": "^0.7.7",
    "sqlite3": "5.1.6",
    "multer": "2.2.0",
    "crypto": "^1.0.1",
    "mqtt": "^4.3.7"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "@types/node": "^20.4.2",
    "typescript": "^5.0.0",
    "dotenv": "^16.0.0"
  }
}